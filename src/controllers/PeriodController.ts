// import { fetchPeriodEntries, savePeriodEntry } from "@/services/firestore";
import PeriodModel from "@/models/PeriodModel";

// export default {
//   async getPeriods() {
//     return await fetchPeriodEntries();
//   },
//   async savePeriod(date: string, status: string) {
//     const period = new PeriodModel(date, status);
//     await savePeriodEntry(period.date, period.status);
//   },
// };
